{
  "name": "ai-nextjs-project",
  "version": "1.0.0",
  "description": "Project for learning AI integration with Next.js",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  }
}
