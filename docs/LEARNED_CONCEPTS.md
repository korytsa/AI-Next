# Изученные темы и понятия

## 🤖 AI и LLM концепции

### Temperature
**Параметр, влияющий на креативность и случайность ответов модели.**
- Низкое значение (0.1-0.3): более детерминированные, предсказуемые ответы
- Высокое значение (0.7-1.0): более креативные, разнообразные ответы
- Используется при создании запросов к AI API

### Max Tokens
**Максимальное количество токенов в ответе модели.**
- Токен ≈ 0.75 слова (для английского)
- Ограничивает длину ответа
- Важно для контроля стоимости и времени ответа

### System Message / System Prompt
**Инструкции для AI, определяющие его поведение и роль.**
- Отправляется один раз в начале диалога
- Задает контекст, стиль общения, правила
- Не виден пользователю, но влияет на все ответы

### Few-shot Learning
**Обучение модели на примерах через промпты.**
- Показ примеров желаемого поведения в системном сообщении
- Модель учится на примерах без изменения весов
- Эффективный способ настройки поведения без fine-tuning

### Chain of Thought (CoT)
**Пошаговое рассуждение AI для решения сложных задач.**
- Модель показывает процесс мышления перед ответом
- Улучшает качество ответов на сложные вопросы
- Помогает найти ошибки в рассуждениях

### Function Calling
**Механизм вызова внешних функций AI моделью.**
- Модель определяет, когда нужна функция
- Возвращает структурированные данные для вызова
- Позволяет интегрировать AI с внешними API и данными

### Streaming
**Постепенная передача ответа по мере генерации.**
- Ответ приходит частями, а не целиком
- Улучшает UX - пользователь видит ответ сразу
- Реализуется через Server-Sent Events (SSE)

### RAG (Retrieval Augmented Generation)
**Улучшение ответов за счет поиска релевантной информации.**
- Поиск информации в векторной БД
- Добавление найденного контекста в промпт
- Позволяет AI работать с актуальными данными

### Embeddings
**Векторные представления текста в многомерном пространстве.**
- Семантически похожие тексты имеют близкие векторы
- Используется для поиска похожих документов
- Основа для RAG и семантического поиска

### Memory Bank
**Долгосрочное хранилище информации о пользователе и контексте.**
- Сохранение фактов, предпочтений, истории
- Персонализация ответов на основе прошлого опыта
- Позволяет AI "помнить" между сессиями

### Context Window
**Максимальное количество токенов, которые модель может "видеть" одновременно.**
- Ограничивает длину истории диалога
- Требует обрезки старых сообщений
- Разные модели имеют разные размеры окна

### Message Trimming
**Обрезка истории диалога для умещения в context window.**
- Удаление старых сообщений
- Суммаризация для сохранения важной информации
- Критично для длинных диалогов

### Token
**Единица измерения текста для LLM.**
- Не равно слову (обычно меньше)
- Используется для расчета стоимости и лимитов
- Пример: "Hello world" = 2 токена

---

## 🛠 Технические концепции

### Server-Sent Events (SSE)
**Односторонний поток данных от сервера к клиенту.**
- Используется для streaming ответов
- Проще чем WebSockets для односторонней передачи
- Автоматическое переподключение при обрыве

### ReadableStream
**API для чтения потоковых данных в JavaScript.**
- Позволяет обрабатывать данные по частям
- Используется для чтения SSE потока
- Асинхронная обработка без блокировки

### Web Speech API
**Браузерный API для распознавания и синтеза речи.**
- `SpeechRecognition` - распознавание речи (голосовой ввод)
- `SpeechSynthesis` - синтез речи (голосовой вывод)
- Поддержка разных языков и голосов

### Speech Recognition
**Преобразование речи в текст.**
- Работает в реальном времени
- Поддерживает разные языки
- Требует разрешения на использование микрофона

### Speech Synthesis
**Преобразование текста в речь.**
- Доступные голоса зависят от браузера и ОС
- Настройка скорости, тона, громкости
- Автоматический выбор голоса по языку

### LocalStorage
**Клиентское хранилище данных в браузере.**
- Сохранение данных между сессиями
- Ограничение ~5-10MB
- Синхронный API, работает только со строками

### SessionStorage
**Временное хранилище данных на время сессии.**
- Очищается при закрытии вкладки
- Похож на localStorage, но с ограниченным временем жизни
- Полезен для временных данных

### Web Workers
**Фоновые потоки для выполнения тяжелых вычислений.**
- Не блокируют основной UI поток
- Нет доступа к DOM, window, localStorage
- Коммуникация через postMessage
- Используется для экспорта больших данных

### Hydration (Next.js)
**Процесс "оживления" статического HTML React компонентами.**
- SSR генерирует HTML на сервере
- На клиенте React "гидратирует" этот HTML
- Может вызывать проблемы с localStorage при первой загрузке

### API Routes (Next.js)
**Серверные endpoints в Next.js App Router.**
- Файлы `route.ts` в папке `app/api/`
- Обработка HTTP запросов (GET, POST, etc.)
- Выполняются на сервере

### Rate Limiting
**Ограничение количества запросов от одного источника.**
- Защита от злоупотреблений
- Предотвращение перегрузки сервера
- Обычно измеряется в запросах за период времени

### Throttling
**Ограничение частоты выполнения операций.**
- Похож на rate limiting, но для конкретных действий
- Используется для оптимизации производительности
- Пример: ограничение частоты API запросов

### Exponential Backoff
**Стратегия повторных попыток с увеличивающейся задержкой.**
- При ошибке ждем 1s, затем 2s, 4s, 8s...
- Предотвращает перегрузку при временных сбоях
- Используется в retry механизмах

### Content Moderation
**Проверка контента на нежелательные материалы.**
- Фильтрация токсичного, оскорбительного контента
- Защита от вредоносных запросов
- Может использовать AI модели модерации

### Prompt Injection
**Атака, при которой пользователь пытается изменить системный промпт.**
- Ввод специальных команд в пользовательский запрос
- Может изменить поведение AI
- Защита через валидацию и sanitization

### Sanitization
**Очистка пользовательского ввода от опасных данных.**
- Удаление HTML тегов, скриптов
- Экранирование специальных символов
- Защита от XSS и injection атак

### Debouncing
**Отложенное выполнение функции после прекращения активности.**
- Функция выполняется только после паузы в действиях
- Используется для оптимизации поиска, валидации
- Пример: поиск срабатывает через 300ms после остановки ввода

### Lazy Loading
**Загрузка данных по требованию, а не все сразу.**
- Улучшает начальную скорость загрузки
- Загружает данные по мере необходимости
- Пример: загрузка старых сообщений при прокрутке

### Pagination
**Разбиение больших списков на страницы.**
- Загрузка данных порциями
- Улучшает производительность
- Пример: загрузка истории сообщений по частям

### Caching
**Сохранение результатов для повторного использования.**
- Уменьшает количество запросов к API
- Ускоряет ответы
- Экономит токены и деньги
- Может быть на разных уровнях (браузер, сервер, CDN)

### Error Boundary
**React компонент для обработки ошибок в дочерних компонентах.**
- Перехватывает ошибки рендеринга
- Показывает fallback UI вместо краша
- Предотвращает полный сбой приложения

### Retry Mechanism
**Автоматическое повторение запроса при ошибке.**
- Полезно для временных сбоев сети
- Обычно с exponential backoff
- Ограничение количества попыток

## 📊 Мониторинг и аналитика

### Metrics
**Метрики использования и производительности.**
- Количество запросов
- Использование токенов
- Время ответа
- Стоимость API вызовов
- Успешность/ошибки

### Error Tracking
**Отслеживание и логирование ошибок.**
- Запись ошибок с контекстом
- Группировка похожих ошибок
- Уведомления о критических ошибках
- Помогает в отладке и улучшении

### Performance Monitoring
**Мониторинг производительности приложения.**
- Время загрузки страниц
- Время ответа API
- Производительность рендеринга
- Выявление узких мест

### Cost Tracking
**Отслеживание расходов на AI API.**
- Подсчет токенов
- Расчет стоимости по тарифам
- Бюджеты и лимиты
- Оптимизация использования

---

## 🔐 Безопасность

### API Key Security
**Безопасное хранение и использование API ключей.**
- Хранение в environment variables
- Никогда не коммитить в git
- Маскирование в логах
- Валидация на сервере

### Environment Variables
**Переменные окружения для конфигурации.**
- Хранение секретов (API ключи, пароли)
- Разные значения для dev/prod
- Файлы `.env.local` не коммитятся

### Input Validation
**Проверка пользовательского ввода.**
- Проверка формата данных
- Валидация длины, типа
- Защита от некорректных данных

### XSS (Cross-Site Scripting)
**Атака через внедрение вредоносных скриптов.**
- Защита через sanitization
- Экранирование HTML
- Content Security Policy

---

## 🗂 Архитектурные паттерны

### Custom Hooks
**Переиспользуемая логика в React хуках.**
- Инкапсуляция сложной логики
- Переиспользование между компонентами
- Упрощение компонентов
- Пример: `useChat`, `useSpeechSynthesis`

### Context API
**Глобальное управление состоянием в React.**
- Избежание prop drilling
- Общий доступ к данным
- Пример: `LanguageContext` для языка интерфейса

### State Management
**Управление состоянием приложения.**
- Локальное состояние (useState)
- Глобальное состояние (Context, Redux)
- Серверное состояние (React Query, SWR)

### Separation of Concerns
**Разделение ответственности между компонентами.**
- UI компоненты отдельно от логики
- Хуки для бизнес-логики
- Утилиты для вспомогательных функций
- API routes для серверной логики
